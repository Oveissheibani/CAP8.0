"""DEPRECATED — retired 2026-05-29.

The auto-grid pair picker that lived here was replaced by the particle-pool +
explicit pair composer in ``particles.py`` (ParticlesPairsPanel).  This module
is intentionally empty; nothing imports it.  Kept as a tombstone so the change
is discoverable in history.
"""
